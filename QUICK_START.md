# Quick Start

1. Open terminal in the project folder.
2. Run `npm install`.
3. Run `npm run dev`.
4. Visit `http://localhost:3000`.

## Change products
Edit `src/data/products.js`.

## Change WhatsApp number
Edit `src/utils/whatsappHelper.js`.

## Change theme colors
Edit CSS variables in `src/index.css`.
